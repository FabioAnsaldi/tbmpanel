'use strict';

const path = require( 'path' );

/**
 *  The main paths of your project handle these with care
 */
exports.paths = {
    configuration: 'config',
    source: 'src',
    production: 'build',
    development: '/dist/',
    bundle: 'bundle.js'
};