'use strict';

import * as homeActions from './actions';

export const something = ( state ) => {
    let props = state.homeReducer;
    /**
     * TODO something function
     * state.dispatch( homeActions.someAction( result ) );
     */
};
