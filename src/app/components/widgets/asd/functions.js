'use strict';

import * as asdActions from './actions';

const doSomething = ( props ) => {
    /* TODO */
    return false;
};

export const something = ( state ) => {
    let result = doSomething( state.asdReducer );
    /**
     * TODO
     * state.dispatch( asdActions.someAction( result ) );
     */
};
